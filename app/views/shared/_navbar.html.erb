<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
  <style>
    body {
      font-family: 'Arial', sans-serif;
      background-color: #fff; 
      color: #000; 
      margin: 0;
      padding: 0;
    }
    .navbar {
      background-color: #fff; 
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    }
    .navbar-toggler {
      border-color: #fff;
      background-color: transparent !important;
    }
    .navbar-toggler-icon {
      background-color: #fff;
    }
    .navbar-nav {
      margin-left: auto;
    }
    .navbar-nav .profile-link {
      color: #000;
      margin: 0 10px;
      padding: 10px 15px;
      border-radius: 5px;
      transition: background-color 0.3s ease;
       display: block;
    }
    .navbar-nav .profile-link:hover {
      background-color: #555;
    }

    .navbar-brand {
      font-family: 'Georgia', serif;
      font-size: 24px;
      color: #000;
    }
  </style>
  <title></title>
</head>
<body>

<nav class="navbar navbar-light">
  <div class="container">
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <% if user_signed_in? %>
      <button class="navbar-toggler ml-auto" type="button" data-bs-toggle="collapse" data-bs-target="#profileMenu" aria-controls="profileMenu" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
    <% end %>

    <div class="collapse navbar-collapse" id="profileMenu">
      <ul class="navbar-nav">
        <li class="nav-item">
          <a class="profile-link" href="<%= personal_information_path %>">
            User Profile
          </a>
          <a class="profile-link" href="<%= edit_user_registration_path %>">Change Password</a>
          <% if user_signed_in? %>
            <%= button_to 'Sign Out', destroy_user_session_path, method: :delete, data: { turbo: false }, class: 'nav-link btn btn-link text-center' %>

          <% end %>
        </li>
      </ul>
    </div>


    <div class="collapse navbar-collapse" id="navbarNavDropdown">
        <ul class="navbar-nav">
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="<%= home_path %>">Home</a>
          <a class="nav-link" href="<%= items_path %>">Shop</a>
          <a class="nav-link" href="<%= wishlists_path %>">Wishlist</a>
          <a class="nav-link" href="<%= carts_path %>">Cart</a>
          <a class="nav-link" href="<%= orders_path %>">Orders</a>
          </li>
      </ul>
    </div>
  </div>
</nav>

</body>
</html>

         
   
    

