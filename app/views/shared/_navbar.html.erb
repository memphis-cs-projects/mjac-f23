<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
  <style>
    body {
      font-family: 'Arial', sans-serif;
      background-color: #2d2d2d; 
      color: #fff; 
      margin: 0;
      padding: 0;
    }

    .navbar {
      background-color: #333; 
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    }

    .navbar-toggler {
      border-color: #fff;
    }

    .navbar-toggler-icon {
      background-color: #fff;
    }

    .navbar-nav {
      margin-left: auto;
    }

    .navbar-nav .nav-link {
      color: #fff;
      margin: 0 10px;
      padding: 10px 15px;
      border-radius: 5px;
      transition: background-color 0.3s ease;
    }

    .navbar-nav .nav-link:hover {
      background-color: #555;
    }

    .navbar-nav .nav-link.active {
      background-color: #555;
    }

    .navbar-nav .nav-link.btn-link {
      padding: 8px 15px;
      border: 1px solid #fff;
    }

    .navbar-nav .nav-item.dropdown:hover .nav-link {
      background-color: #555;
    }

    .navbar-nav .nav-item.dropdown .dropdown-menu {
      background-color: #333;
    }

    .navbar-nav .nav-item.dropdown .dropdown-item {
      color: #fff;
    }

    .navbar-nav .nav-item.dropdown .dropdown-item:hover {
      background-color: #555;
    }

    .navbar-brand {
      font-family: 'Georgia', serif;
      font-size: 24px;
      color: #fff;
    }

   
    .profile-badge {
      background-color: #28a745; /
      border-radius: 20px;
      padding: 5px 15px;
      margin-right: 10px;
    }
  </style>
  <title></title>
</head>
<body>

<nav class="navbar navbar-light">
  <div class="container">
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavDropdown">
        <ul class="navbar-nav">
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="<%= home_path %>">Home</a>
          <a class="nav-link" href="<%= items_path %>">Shop</a>
          <a class="nav-link" href="<%= wishlists_path %>">Wishlist</a>
          <a class="nav-link" href="<%= carts_path %>">Cart</a>
          <a class="nav-link" href="<%= orders_path %>">Orders</a>
          <% if user_signed_in? %>
            <a class="nav-link" href="<%= personal_information_path %>">
              <span class="profile-badge">Profile</span> User Profile
            </a>
            <a class="nav-link" href="<%= edit_user_registration_path %>">Change password</a>
          <% end %>
        </li>
      </ul>
   
      </ul>

      <ul class="navbar-nav">
        <% if user_signed_in? %>
          <li class="nav-item">
            <%= link_to "Hi, #{current_user.full_name}", edit_user_registration_path, class: "nav-link btn btn-link" %>
          </li>
          <li class="nav-item">
            <%= link_to "User Profile", personal_information_path, class: "nav-link" %>
          </li>
          </li>
          <li class="nav-item">
            <%= button_to 'Sign Out', destroy_user_session_path, method: :delete, data: { turbo: false }, class: 'nav-link btn btn-link' %>
        <% else %>
          <li class="nav-item">
            <%= link_to 'Sign In', new_user_session_path, class: "nav-link btn btn-link" %>
          </li>
          <li class="nav-item">
            <%= link_to 'Sign Up', new_user_registration_path, class: "nav-link btn btn-link" %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>


